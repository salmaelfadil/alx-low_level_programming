#include "main.h"
/**
 * rev_string - reverses a string
 * @s: string to be reversed
 *
 * Return: void;
 */
void rev_string(char *s)
{
	int i, j;

	i = 0;

	while (s[i])
	{
		i++;
	}

	char k[i];

	for (; i != 0; i--)
	{
		k = s[i];
	}
}
